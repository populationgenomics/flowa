{
  "name": "@flowajs/prompts",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace anchor for prompt sets consumed by @flowajs/chat-service. Each subdirectory is a self-contained set; its aggregation artifacts (edit schema, templates, manifest, modules) live under <set>/aggregation/.",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./*": "./*/aggregation/edit_schema.ts",
    "./*/categories.json": "./*/aggregation/categories.json"
  },
  "dependencies": {
    "@flowajs/chat-service": "workspace:*",
    "zod": "4.4.3"
  }
}
